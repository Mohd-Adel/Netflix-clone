// import { selectedMovie } from "../Actions/types";

// const initialState = () => ({
//   selectedMovieState: []
// });

// const selectedMovieReducer = (state = initialState, action) => {
//   switch (action.type) {
//     case selectedMovie:
//       return (state = {
//         ...state,
//         selectedMovie: action.payload
//       });
//     default:
//       return state;
//   }
// };

// export default selectedMovieReducer;
